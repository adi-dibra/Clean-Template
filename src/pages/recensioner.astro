---
import Layout from '../layouts/Layout.astro';

const reviews = [
  {
    quote: "Excellent service and professional staff.",
    author: "John D.",
    role: "Project Client",
    rating: 5
  },
  {
    quote: "Great experience working with this company. Highly recommended.",
    author: "Sarah M.",
    role: "Business Client",
    rating: 5
  }
];


---

<Layout title="Kundrecensioner - NG STHLM AB">
  <!-- Hero Section -->
  <section class="relative py-20 bg-secondary text-white">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl" data-aos="fade-up">
        <h1 class="text-5xl font-bold mb-6">Vad Våra Kunder Säger</h1>
        <p class="text-xl text-gray-300">
          Upptäck varför ledande företag och organisationer väljer NG STHLM AB för sina byggprojekt
        </p>
      </div>
    </div>
  </section>

  <!-- Reviews Grid -->
  <section class="py-20">
    <div class="container mx-auto px-4">
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {reviews.map(review => (
          <div class="bg-white p-8 rounded-lg shadow-lg" data-aos="fade-up">
            <!-- Rating Stars -->
            <div class="flex mb-4">
              {Array.from({ length: review.rating }).map(() => (
                <svg class="w-5 h-5 text-primary" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                </svg>
              ))}
            </div>
            
            <blockquote class="text-gray-700 mb-6 italic">"{review.quote}"</blockquote>
            
            <div class="flex items-center">
              <div>
                <div class="font-semibold text-secondary">{review.author}</div>
                <div class="text-gray-600 text-sm">{review.role}</div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Leave Review CTA -->
      <div class="mt-20 text-center" data-aos="fade-up">
        <div class="max-w-2xl mx-auto bg-accent p-8 rounded-lg shadow-lg">
          <h2 class="text-2xl font-bold text-secondary mb-4">Har du ett projekt med oss?</h2>
          <p class="text-gray-600 mb-6">
            Vi värdesätter feedback från våra kunder. Dela din erfarenhet och hjälp andra att göra ett välgrundat val.
          </p>
          <a
            href="/contact?type=review"
            class="inline-block bg-primary hover:bg-primary/90 text-white px-8 py-3 rounded-none transition-colors"
          >
            Lämna en Recension
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>